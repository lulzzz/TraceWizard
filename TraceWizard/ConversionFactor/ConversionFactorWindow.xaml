<Window x:Class="TraceWizard.TwApp.ConversionFactorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:TraceWizard.TwApp"        
    ShowInTaskbar = "False" 
    SizeToContent="Height"
    WindowStartupLocation="CenterOwner"
    WindowStyle="ToolWindow"
    MaxWidth="300"
    MinWidth="300"
        Title="Apply Conversion Factor"
FocusManager.FocusedElement="{Binding ElementName=TextBoxConversionFactor}"        
    >
    <StackPanel Margin="20">
        <DockPanel HorizontalAlignment="Stretch">
            <TextBlock DockPanel.Dock="Left" Text="Conversion Factor:" Padding="0,0,6,0" VerticalAlignment="Center" />
            <TextBox DockPanel.Dock="Right" Name="TextBoxConversionFactor" VerticalAlignment="Center" HorizontalAlignment="Stretch" />
        </DockPanel>
        <DockPanel HorizontalAlignment="Right" LastChildFill="False" Margin="10,10,0,0">
            <Button DockPanel.Dock="Right" Name="ButtonCancel" Content="Cancel" MinWidth="75" Margin="6,6,0,6" IsCancel="True"
            HorizontalAlignment="Right" HorizontalContentAlignment="Center" />
            <Button DockPanel.Dock="Right" Name="ButtonOk" Content="OK" MinWidth="75" Margin="6" IsDefault="True" 
            HorizontalAlignment="Right" HorizontalContentAlignment="Center" />
        </DockPanel>
    </StackPanel>
</Window>
    